Сложность https://github.com/Phytophthorida/Labs/blob/main/AISTO/.
AISTO/AISTO.cpp#L17 в худшем случае O(1), потому что количество операций не зависит от размера входных данных.

Сложность https://github.com/Phytophthorida/Labs/blob/main/AISTO/.
AISTO/AISTO.срр#L51 в худшем случае O(n), потому что выделение памяти для нового массива и копирования элементов из старого в новый оценивается как O(n), так как функция проходит по массиву из n элементов.

Сложность https://github.com/Phytophthorida/Labs/blob/main/AIST1.1/.
AIST1.1/AIST1.1.cpp#L85 в худшем случае O(n), потому что создание массива tokens и заполнение его значениями, а также  цикл, в котором каждый токен обрабатывается, можно оценить как О(n), так как фукция обрабатывает n токенов.